<?xml version="1.0"?>
<psalm
    phpVersion="8.4"
    errorLevel="7"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    allConstantsGlobal="true"
    ensureOverrideAttribute="false"
    errorBaseline="psalm-baseline.xml"
>
    <projectFiles>
        <directory name="."/>
        <!-- allowMissingFiles : node_modules/vendor n'existent pas dans un worktree frais -->
        <ignoreFiles allowMissingFiles="true">
            <directory name=".claude"/>
            <directory name="docker"/>
            <directory name="node_modules"/>
            <directory name="resources"/>
            <directory name="tests"/>
            <directory name="var"/>
            <directory name="web"/>
            <directory name="vendor"/>
        </ignoreFiles>
    </projectFiles>
    <plugins>
        <pluginClass class="Orklah\PsalmInsaneComparison\Plugin"/>
    </plugins>
    <!--
    Globales du legacy : définies par app/config.php et app/bootstrap.php, puis
    récupérées via `global $x;` dans les pages. Sans cette liste Psalm les voit
    comme UndefinedGlobalVariable. Garder synchronisé avec ces deux fichiers.
    -->
    <globals>
        <!-- app/bootstrap.php : objets de service -->
        <var name="connector" type="Ladecadanse\Utils\DbConnector"/>
        <var name="connectorPdo" type="Ladecadanse\Utils\DbConnectorPdo"/>
        <var name="authorization" type="Ladecadanse\Security\Authorization"/>
        <var name="videur" type="Ladecadanse\Security\Sentry"/>
        <var name="tplEngine" type="Ladecadanse\TemplateEngine"/>
        <var name="translator" type="Ladecadanse\Translator"/>
        <var name="assets" type="Ladecadanse\Utils\AssetManager"/>
        <var name="logger" type="Monolog\Logger"/>
        <var name="loggerApi" type="Monolog\Logger"/>
        <var name="site_full_url" type="string"/>
        <var name="nom_page" type="string"/>

        <!-- app/config.php : dates -->
        <var name="auj" type="string"/>
        <var name="glo_auj" type="string"/>
        <var name="glo_auj_6h" type="string"/>

        <!-- app/config.php : tableaux du domaine -->
        <var name="glo_regions" type="array&lt;string, string&gt;"/>
        <var name="glo_categories_lieux" type="array&lt;string, string&gt;"/>
        <var name="glo_menu_pratique" type="array&lt;string, string&gt;"/>
        <var name="glo_tab_genre" type="array&lt;string, string&gt;"/>
        <var name="glo_tab_ailleurs" type="array&lt;string, string&gt;"/>
        <var name="glo_tab_quartiers" type="list&lt;string&gt;"/>
        <var name="glo_tab_quartiers2" type="array&lt;string, list&lt;string&gt;&gt;"/>
        <var name="mimes_images_acceptes" type="list&lt;string&gt;"/>
        <var name="mimes_documents_acceptes" type="list&lt;string&gt;"/>

        <!-- app/config.php : chemins d'upload -->
        <var name="rep_images_even" type="string"/>
        <var name="rep_uploads_lieux" type="string"/>
        <var name="rep_uploads_lieux_galeries" type="string"/>
        <var name="rep_uploads_organisateurs" type="string"/>
        <var name="rep_fichiers_lieu" type="string"/>

        <!-- app/config.php : URLs et fragments d'interface -->
        <var name="url_images_interface_icons" type="string"/>
        <var name="url_query_region" type="string"/>
        <var name="url_query_region_et" type="string"/>
        <var name="url_query_region_1er" type="string"/>
        <var name="icone" type="array&lt;string, string&gt;"/>
        <var name="iconeEditer" type="string"/>
        <var name="iconeCopier" type="string"/>

        <!-- définies par les pages elles-mêmes avant inclusion de _header/_footer -->
        <var name="page_titre" type="string"/>
        <var name="evenement" type="array"/>
        <var name="calLinks" type="array"/>
    </globals>
</psalm>
<!--
Level 1 is the most strict, level 8 is the most lenient.

When no level is explicitly defined, psalm defaults to level 2.

Some issues are always treated as errors. These are issues with a very low probability of false-positives.

At level 1 all issues (except those emitted for opt-in features) that Psalm can find are treated as errors. Those issues include any situation where Psalm cannot infer the type of a given expression.

At level 2 Psalm ignores those Mixed* issues, but treats most other issues as errors.

At level 3 Psalm starts to be a little more lenient. For example Psalm allows missing param types, return types and property types.

At level 4 Psalm ignores issues for possible problems. These are more likely to be false positives – where the application code may guarantee behaviour that Psalm isn't able to infer.

Level 5 and above allows a more non-verifiable code, and higher levels are even more permissive.
-->